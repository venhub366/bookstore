server:
  port : 3034
  shutdown : GRACEFUL

management:
  info:
    git:
      mode: FULL
  endpoints:
    web:
      exposure:
        include : '*'

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:15432/postgres}
    username: ${DB_NAME:admin}
    password: ${DB_PWD:admin}}
