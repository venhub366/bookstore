server:
  port : 3034
  shutdown : GRACEFUL

management:
  endpoints:
    web:
      exposure:
        include : '*'

spring:
  datasource:
    url : jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username : sa
    password : password
  h2:
    console:
      enabled : true
